你现在负责这个金融机器学习论文项目的工程实现。

请严格遵守以下通用规则：

## 0. 总体原则

1. 先阅读当前 repo 中已有的所有相关代码、配置、脚本、测试与文档；如果 repo 近乎空白，就先检查目录结构与已有数据文件，再开始修改。
2. 只做当前阶段要求的内容，不要提前实现后续阶段，不要擅自扩展任务范围。
3. 优先复用现有实现，遵循“最小必要修改”原则；不要为了“更优雅”而重写已经可用的模块。
4. 所有新增实现都要与现有项目的目录结构、命名风格、配置风格、日志风格保持一致。
5. 不要擅自改变论文主线设定、实验协议、数据口径和阶段编号；若发现现有实现与既定原则冲突，应优先修正为既定原则，并在输出中明确说明。

## 1. 阶段边界

6. 阶段编号固定如下，不要混用，也不要跳阶段实现：
   - 阶段0：repo骨架
   - 阶段1：外部数据准备
   - 阶段2：数据标准化与基础映射
   - 阶段3：特征、标签与sample_index底座
   - 阶段4：可行性验证
   - 阶段5：全量抽取流水线
   - 阶段6：语义图与价格图
   - 阶段7：训练框架
   - 阶段8：Temporal-only 与 MASTER
   - 阶段9：A2~A6 主实验矩阵
   - 阶段10：RSR、经典baseline与backbone robustness
   - 阶段11：论文图表与结果汇总

7. 当前任务若属于某一阶段，只允许实现该阶段直接需要的内容；不要顺手实现后续阶段的接口、脚本、实验、图模块或训练逻辑，除非当前阶段明确依赖。
8. 若某个需求明显属于后续阶段，应在输出中指出“该内容属于后续阶段，本次不实现”。

## 2. 时间与防泄露原则

9. 实验总评估区间固定为：
   - 2016-01-01 到 2023-12-31
10. warm-up 数据准备起点固定为：
   - 2015-09-01
11. warm-up 数据只用于窗口初始化，不能进入任何 train / val / test 样本。
12. 所有图构建、样本构建、特征切片、标签生成函数，都必须显式接受 `cutoff_date` 参数。
13. 所有依赖时间的数据处理逻辑，都必须有断言，确保不使用 `cutoff_date` 之后的数据。
14. 如果当前阶段尚未正式进入图构建或样本构建，也要避免写出未来可能默认泄露时间信息的接口。
15. 若发现现有代码存在时间穿越、未来函数、标签泄露、窗口越界或 warm-up 混入训练样本的问题，必须优先修正，并在输出中写清楚修正点。

## 3. 数据口径与原始数据处理

16. FNSPID 原始数据已经存在，本阶段不要重新下载 FNSPID，不要改写原始文件，不要移动或覆盖原始数据。
17. 本项目的原始数据、interim 数据、processed 数据要分层清晰；不要把处理后的中间产物写回 raw 目录。
18. 如果本阶段涉及大文件处理，应优先采用流式/分块读取，避免一次性把超大文件全部载入内存。
19. 未经明确要求，不要擅自合并额外数据源、增加外部数据、替换主语料或修改既定数据口径。
20. 若发现原始数据的真实字段名、目录结构、文件格式与预期不一致，应先适配真实数据结构，再继续实现；并在输出中说明发现了什么差异、如何适配。

## 4. 配置与常量管理

21. 如果需要新增配置，请优先写入 `configs/` 下的 yaml/json/txt 文件，不要把关键常量硬编码到脚本里。
22. 下列内容原则上不得硬编码在业务逻辑中：
   - 日期边界
   - 路径
   - 文件名
   - 窗口长度
   - 阈值
   - 列名映射
   - 运行模式
23. 新增配置时，要尽量复用已有配置体系，不要平行再造一套新的配置入口。
24. 若确实必须在代码中保留少量默认值，应保证默认值与配置文件一致，并在输出中说明原因。

## 5. 方法边界（禁止越界改设定）

25. 不要越界修改论文方法设定。以下原则必须保持不变：
   - LLM 只做 `source / targets / event_type` 抽取
   - 不允许 `polarity / strength / confidence`
   - 语义边不是文本相似度边
   - 边权优先采用 binary 或 time-decayed count
26. 不要在当前阶段偷偷加入与主线不一致的替代设计，例如：
   - 用文本相似度代替事件溢出边
   - 用情感分数替代结构抽取
   - 用 LLM 输出的置信度做边权
27. 若发现历史代码里存在这些冲突实现，应保留必要兼容性前提下修正到当前主线，并在输出中说明。

## 6. 工程实现要求

28. 优先保证正确性、可复现性和与论文设定一致，再考虑额外优化。
29. 不要做与当前任务无关的大规模重构。
30. 新增代码应具备清晰的函数边界、必要注释和基本错误处理。
31. 新增脚本应支持最小可复现运行；若需要命令行参数，优先使用已有 CLI 风格。
32. 若某一步执行耗时长、资源重，应优先提供可 smoke 的最小运行路径，同时保留全量运行方式。
33. 涉及大表处理时，应尽量输出统计信息、数据覆盖情况和关键异常计数，便于后续审计。

## 7. 测试与验证

34. 修改后必须运行本阶段最小 smoke test；如果没有测试，就补一个最小 smoke test。
35. smoke test 应尽量覆盖：
   - 关键输入路径是否可读
   - 关键配置是否生效
   - 关键函数是否能跑通
   - 关键输出是否生成
   - 关键字段/日期约束是否满足
36. 若本阶段已有相关单元测试、集成测试或 smoke 测试，应优先复用并补齐，而不是绕开测试。
37. 输出中必须明确写出：
   - 运行了哪些测试/命令
   - 是否通过
   - 若未通过，卡在哪里
38. 不要声称“应该可以跑通”；必须区分“已实际跑通”和“仅代码层面完成但未执行验证”。

## 8. 输出与汇报格式

39. 每次完成任务后，输出必须包含以下部分：
   - 改了什么
   - 为什么这么改
   - 修改了哪些文件
   - 如何运行
   - 是否跑通
   - 产物路径
   - 关键统计/结果
   - 剩余风险
   - 下一步建议
40. 如果只完成了部分内容，要明确哪些完成了，哪些没有完成，不要模糊表述。
41. 如果因为数据、资源、依赖、时间或环境问题无法完成某一步，要给出明确原因，不要假装已经完成。
42. 若新增了中间产物或输出文件，要说明其用途、路径和是否应纳入后续阶段输入。

## 9. 文档与可追踪性

43. 若本阶段引入了新的脚本、配置、数据产物或测试，需补充最小必要文档说明，确保后续能够复现。
44. 文档不需要写成论文，但至少要说明：
   - 输入是什么
   - 输出是什么
   - 怎么运行
   - 有哪些关键约束
45. 若修改了已有逻辑的行为，应在输出中标明这是“修复”“重构”“补充测试”还是“新增能力”。

## 10. 禁止事项

46. 不要重新下载或覆盖已有原始数据。
47. 不要擅自修改论文任务定义、评估区间、baseline 体系或阶段划分。
48. 不要在当前阶段引入训练、回测、图建模、LLM 抽取或论文写作等后续阶段内容，除非当前任务明确要求。
49. 不要静默删除已有文件、配置或测试；如必须删除或替换，需在输出中明确说明原因。
50. 不要省略防泄露约束、warm-up 约束、数据边界检查和最小 smoke test。